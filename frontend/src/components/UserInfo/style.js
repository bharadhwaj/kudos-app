const style = theme => ({
  userInfoElement: {
    paddingTop: theme.spacing(1),
  },
  userInfoIcon: {
    color: '#757575',
    marginRight: theme.spacing(2),
  },
  logoutButton: {
    paddingTop: theme.spacing(4),
  },
});

export default style;
